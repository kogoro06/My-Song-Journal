<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Myapp" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-gray-100 flex flex-col min-h-screen">
    <header class="bg-blue-600 text-white p-4">
      <div class="container mx-auto flex justify-between items-center">
        <h1 class="text-2xl font-bold">TODOアプリ</h1>
        <div>
          <% if user_signed_in? %>
            <span class="mr-4">こんにちは、<%= current_user.email %> さん</span>
            <%= link_to "マイページ", edit_user_registration_path, class: "mr-4 text-white hover:underline" %>
            <%= button_to 'ログアウト', destroy_user_session_path, method: :delete , class: "text-white hover:underline" %>
          <% else %>
            <%= link_to "サインイン", new_user_session_path, class: "mr-4 text-white hover:underline" %>
            <%= link_to "サインアップ", new_user_registration_path, class: "text-white hover:underline" %>
          <% end %>
        </div>
      </div>
    </header>
    
    <!-- フラッシュメッセージの追加 -->
    <div class="container mx-auto mt-4">
      <% flash.each do |type, message| %>
        <% alert_class = type == 'notice' ? 'bg-green-100 border-green-400 text-green-700' : 'bg-red-100 border-red-400 text-red-700' %>
        <div class="border-l-4 p-4 mb-4 <%= alert_class %>">
          <%= message %>
        </div>
      <% end %>
    </div>
    
    <main class="container mx-auto mt-8 flex-grow">
      <%= yield %>
    </main>
    <footer class="bg-gray-800 text-white text-center p-4 mt-8">
      <p>&copy; 2024 TODOアプリ. All Rights Reserved.</p>
    </footer>
  </body>
</html>
